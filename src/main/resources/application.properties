# Application Configuration
spring.application.name=Paperless

# Server Configuration
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/paperless_db}
spring.datasource.username=${DB_USERNAME:paperless_user}
spring.datasource.password=${DB_PASSWORD:Paperless@123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# File Upload Configuration
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.enabled=true

# JWT Configuration
jwt.secret=${JWT_SECRET:MySecureJWTSecretKey123456789ForPaperlessApplication2024}
jwt.expiration=86400000

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:22472897816-nga9oac90l03t9800a1ts8celogmlo0a.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# File Storage Path
file.upload-dir=${FILE_UPLOAD_DIR:./uploads}
